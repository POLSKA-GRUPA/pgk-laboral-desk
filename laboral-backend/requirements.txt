fastapi>=0.115
python-multipart>=0.0.9
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.0
pydantic-settings>=2.0
python-jose[cryptography]>=3.3
bcrypt>=4.0
httpx>=0.28
weasyprint>=62.0
structlog>=24.0
lxml>=5.0

# MCP server (stdio + SSE) for Claude Desktop / Cursor / Windsurf.
# Ver app/mcp/server_main.py y docs/MCP_INTEGRATION.md.
mcp>=1.0

# Agente CodeAct (/api/agent/*). Los providers son opcionales en tiempo de
# ejecucion — si no hay API key configurada el endpoint /api/agent/status
# devuelve available=false, pero las libs viajan con el paquete para poder
# activar el agente cambiando una variable de entorno, sin rebuilds.
langchain-core>=0.3
langchain-google-genai>=2.0
langchain-anthropic>=0.3
